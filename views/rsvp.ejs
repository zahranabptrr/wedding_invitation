<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RSVP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', 'Poppins', sans-serif;
            background: url('/images/background.jpg') no-repeat center center fixed;
            background-size: cover;
            min-height: 100vh;
            position: relative;
        }
        body::before {
            content: '';
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            background: linear-gradient(135deg, #f5e9d7cc 0%, #e7d3c0cc 100%);
            z-index: 1;
            pointer-events: none;
        }
        .main-content {
            position: relative;
            z-index: 2;
            max-width: 500px;
            margin: 0 auto;
            background: rgba(255,255,255,0.92);
            border-radius: 32px;
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.15);
            padding: 2.5rem 2rem;
            margin-top: 4rem;
            margin-bottom: 2.5rem;
            border: 1.5px solid #e2c9b0;
            text-align: center;
        }
        .section-title {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            color: #8B4513;
            margin-bottom: 2rem;
        }
        .rsvp-form input, .rsvp-form select {
            background: rgba(255,255,255,0.95);
            border: 1.5px solid #e2c9b0;
            border-radius: 10px;
            padding: 12px;
            margin-bottom: 1rem;
        }
        .btn-next {
            background: linear-gradient(45deg, #bfa181, #8B4513);
            border: none;
            padding: 14px 40px;
            border-radius: 50px;
            color: white;
            font-weight: 600;
            font-size: 1.1rem;
            margin-top: 2rem;
            transition: all 0.3s;
        }
        .btn-next:hover {
            background: linear-gradient(45deg, #8B4513, #bfa181);
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="main-content">
        <div class="section-title">RSVP</div>
        <form action="/rsvp" method="POST" class="rsvp-form">
            <div class="mb-3">
                <input type="text" class="form-control" name="nama" placeholder="Nama Anda" required>
            </div>
            <div class="mb-3">
                <input type="number" class="form-control" name="jumlah" placeholder="Jumlah Tamu" min="1" value="1" required>
            </div>
            <div class="mb-3">
                <select class="form-control" name="kehadiran" required>
                    <option value="">Pilih Konfirmasi Kehadiran</option>
                    <option value="Hadir">Hadir</option>
                    <option value="Tidak Hadir">Tidak Hadir</option>
                </select>
            </div>
            <button type="submit" class="btn btn-primary w-100 mb-3">Kirim RSVP</button>
        </form>
        <a href="/invitation/wish" class="btn btn-next">Next</a>
    </div>
</body>
</html> 
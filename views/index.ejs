<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Undangan Pernikahan Aqeele & Mohan</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.css" />
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Poppins:wght@300;400;500;600;700&family=Great+Vibes&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background: url('/images/background.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #2c1810;
            line-height: 1.6;
            overflow-x: hidden;
        }
        
        /* Overlay for better readability */
        body::before {
            content: '';
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.9) 0%, rgba(255, 248, 240, 0.95) 100%);
            z-index: 1;
            pointer-events: none;
        }

        /* Floating Flowers Animation */
        .floating-flowers {
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            pointer-events: none;
            z-index: 2;
        }
        .flower-particle { 
            position: absolute; 
            font-size: 1.2rem; 
            opacity: 0.6; 
            animation: flowerFloat 20s linear infinite; 
            color: #d4a574;
        }
        .flower-particle:nth-child(1) { left: 5%; animation-delay: 0s; animation-duration: 25s; }
        .flower-particle:nth-child(2) { left: 15%; animation-delay: 2s; animation-duration: 22s; }
        .flower-particle:nth-child(3) { left: 25%; animation-delay: 4s; animation-duration: 28s; }
        .flower-particle:nth-child(4) { left: 35%; animation-delay: 6s; animation-duration: 24s; }
        .flower-particle:nth-child(5) { left: 45%; animation-delay: 8s; animation-duration: 30s; }
        .flower-particle:nth-child(6) { left: 55%; animation-delay: 10s; animation-duration: 26s; }
        .flower-particle:nth-child(7) { left: 65%; animation-delay: 12s; animation-duration: 32s; }
        .flower-particle:nth-child(8) { left: 75%; animation-delay: 14s; animation-duration: 23s; }
        .flower-particle:nth-child(9) { left: 85%; animation-delay: 16s; animation-duration: 29s; }
        .flower-particle:nth-child(10) { left: 95%; animation-delay: 18s; animation-duration: 27s; }
        @keyframes flowerFloat {
            0% { transform: translateY(100vh) rotate(0deg); opacity: 0; }
            10%,90% { opacity: 0.6; }
            100% { transform: translateY(-100px) rotate(360deg); opacity: 0; }
        }

        /* Main Container */
        .main-container {
            position: relative;
            z-index: 3;
            max-width: 100%;
            margin: 0 auto;
        }

        /* Hero Section */
        .hero-section {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(255, 248, 240, 0.98) 100%);
            position: relative;
            padding: 2rem 1rem;
        }

        .hero-content {
            max-width: 800px;
            margin: 0 auto;
        }

        .invitee-name {
            font-family: 'Great Vibes', cursive;
            font-size: 2.5rem;
            color: #8b4513;
            margin-bottom: 1rem;
            text-shadow: 0 2px 4px rgba(139, 69, 19, 0.1);
        }

        .quran-verse {
            font-family: 'Playfair Display', serif;
            font-size: 1.1rem;
            color: #6b4f36;
            margin-bottom: 2rem;
            font-style: italic;
            line-height: 1.8;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .wedding-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-weight: 400;
            color: #8b4513;
            margin-bottom: 1rem;
            letter-spacing: 3px;
        }

        .couple-names {
            font-family: 'Great Vibes', cursive;
            font-size: 4rem;
            color: #d4a574;
            margin-bottom: 1.5rem;
            text-shadow: 0 4px 8px rgba(212, 165, 116, 0.2);
        }

        .wedding-date {
            font-family: 'Playfair Display', serif;
            font-size: 1.3rem;
            color: #6b4f36;
            margin-bottom: 2rem;
            font-weight: 500;
        }

        /* Countdown */
        .countdown {
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            margin: 2rem 0;
            flex-wrap: wrap;
        }
        .countdown-item {
            text-align: center;
            background: rgba(255, 255, 255, 0.8);
            padding: 1.5rem 1rem;
            border-radius: 20px;
            min-width: 100px;
            border: 2px solid #e2c9b0;
            box-shadow: 0 4px 15px rgba(139, 69, 19, 0.1);
        }
        .countdown-number {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            font-weight: 700;
            display: block;
            color: #8b4513;
            line-height: 1;
        }
        .countdown-label {
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #6b4f36;
            font-weight: 500;
            margin-top: 0.5rem;
        }

        /* Navigation */
        .section-nav {
            position: fixed;
            bottom: 2rem;
            left: 50%;
            transform: translateX(-50%);
            z-index: 1000;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 50px;
            padding: 0.5rem;
            box-shadow: 0 4px 20px rgba(139, 69, 19, 0.15);
            border: 1px solid #e2c9b0;
        }

        .nav-btn {
            background: transparent;
            border: none;
            padding: 0.8rem 1.2rem;
            margin: 0 0.2rem;
            border-radius: 25px;
            color: #6b4f36;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .nav-btn:hover, .nav-btn.active {
            background: linear-gradient(45deg, #d4a574, #8b4513);
            color: white;
        }

        /* Sections */
        .section {
            padding: 4rem 2rem;
            background: rgba(255, 255, 255, 0.9);
            margin: 2rem auto;
            max-width: 1000px;
            border-radius: 30px;
            box-shadow: 0 8px 32px rgba(139, 69, 19, 0.1);
            border: 1px solid #e2c9b0;
        }

        .section-title {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 3rem;
            color: #8b4513;
            font-weight: 600;
            position: relative;
        }

        .section-title::after {
            content: '❦ ❦ ❦';
            display: block;
            font-size: 1.2rem;
            color: #d4a574;
            margin-top: 1rem;
            opacity: 0.8;
        }

        /* Couple Section */
        .couple-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .couple-card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 25px;
            padding: 2.5rem 2rem;
            text-align: center;
            box-shadow: 0 6px 20px rgba(139, 69, 19, 0.08);
            border: 1px solid #e2c9b0;
            transition: transform 0.3s ease;
        }

        .couple-card:hover {
            transform: translateY(-5px);
        }

        .couple-photo {
            width: 180px;
            height: 180px;
            border-radius: 50%;
            object-fit: cover;
            margin: 0 auto 1.5rem;
            border: 4px solid #d4a574;
            box-shadow: 0 8px 25px rgba(139, 69, 19, 0.15);
        }

        .couple-name {
            font-family: 'Great Vibes', cursive;
            font-size: 2.2rem;
            color: #8b4513;
            margin-bottom: 0.5rem;
        }

        .couple-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.1rem;
            color: #d4a574;
            margin-bottom: 1rem;
            font-weight: 500;
        }

        .couple-parents {
            font-size: 0.95rem;
            color: #6b4f36;
            line-height: 1.6;
        }

        /* Event Section */
        .event-card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 25px;
            padding: 2.5rem;
            text-align: center;
            box-shadow: 0 6px 20px rgba(139, 69, 19, 0.08);
            border: 1px solid #e2c9b0;
            margin-bottom: 2rem;
        }

        .event-date {
            font-family: 'Playfair Display', serif;
            font-size: 2.2rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: #8b4513;
        }

        .event-time {
            font-size: 1.2rem;
            margin-bottom: 1.5rem;
            color: #d4a574;
            font-weight: 500;
        }

        .event-location {
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
            color: #6b4f36;
        }

        .location-link {
            color: #8b4513;
            text-decoration: none;
            border-bottom: 2px dotted #d4a574;
            transition: all 0.3s ease;
            font-weight: 500;
        }

        .location-link:hover {
            color: #d4a574;
            border-bottom-color: #8b4513;
        }

        /* Gallery Section */
        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .gallery-item {
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 6px 20px rgba(139, 69, 19, 0.1);
            transition: transform 0.3s ease;
        }

        .gallery-item:hover {
            transform: scale(1.05);
        }

        .gallery-item img {
            width: 100%;
            height: 250px;
            object-fit: cover;
        }

        /* RSVP Section */
        .rsvp-form {
            max-width: 500px;
            margin: 0 auto;
        }

        .rsvp-form input, 
        .rsvp-form select, 
        .wish-input {
            background: rgba(255, 255, 255, 0.95);
            border: 2px solid #e2c9b0;
            border-radius: 15px;
            padding: 1rem;
            margin-bottom: 1.5rem;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .rsvp-form input:focus, 
        .rsvp-form select:focus, 
        .wish-input:focus {
            outline: none;
            border-color: #d4a574;
            box-shadow: 0 0 0 3px rgba(212, 165, 116, 0.1);
        }

        .btn-primary {
            background: linear-gradient(45deg, #d4a574, #8b4513);
            border: none;
            padding: 1rem 2.5rem;
            border-radius: 50px;
            font-weight: 600;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            font-size: 1rem;
        }

        .btn-primary:hover {
            background: linear-gradient(45deg, #8b4513, #d4a574);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(139, 69, 19, 0.2);
        }

        /* Wish Box Section */
        .wish-box {
            max-width: 600px;
            margin: 0 auto;
        }

        .wish-input {
            resize: none;
            min-height: 120px;
        }

        /* Gift Section */
        .gift-info {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 25px;
            padding: 2.5rem;
            text-align: center;
            box-shadow: 0 6px 20px rgba(139, 69, 19, 0.08);
            border: 1px solid #e2c9b0;
        }

        .gift-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            color: #8b4513;
            margin-bottom: 1.5rem;
        }

        .gift-text {
            color: #6b4f36;
            margin-bottom: 2rem;
            line-height: 1.8;
        }

        .bank-info {
            background: rgba(212, 165, 116, 0.1);
            border-radius: 15px;
            padding: 1.5rem;
            margin: 1rem 0;
            border: 1px solid #e2c9b0;
        }

        .bank-name {
            font-weight: 600;
            color: #8b4513;
            margin-bottom: 0.5rem;
        }

        .bank-number {
            font-family: 'Playfair Display', serif;
            font-size: 1.2rem;
            color: #6b4f36;
            font-weight: 500;
        }

        /* Audio Player */
        .audio-player {
            position: fixed;
            top: 2rem;
            right: 2rem;
            z-index: 1000;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 50px;
            padding: 0.8rem 1.5rem;
            box-shadow: 0 4px 20px rgba(139, 69, 19, 0.15);
            border: 1px solid #e2c9b0;
        }

        .audio-player audio {
            width: 200px;
            height: 40px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-section { padding: 1rem; }
            .invitee-name { font-size: 2rem; }
            .couple-names { font-size: 2.5rem; }
            .wedding-title { font-size: 1.4rem; }
            .countdown { gap: 1rem; }
            .countdown-item { min-width: 80px; padding: 1rem 0.5rem; }
            .countdown-number { font-size: 1.8rem; }
            .section { padding: 2rem 1rem; margin: 1rem; }
            .section-title { font-size: 2rem; }
            .couple-photo { width: 140px; height: 140px; }
            .couple-name { font-size: 1.8rem; }
            .audio-player { top: 1rem; right: 1rem; }
            .audio-player audio { width: 150px; }
            .section-nav { bottom: 1rem; padding: 0.3rem; }
            .nav-btn { padding: 0.6rem 1rem; font-size: 0.8rem; }
        }

        /* Smooth Scroll */
        html {
            scroll-behavior: smooth;
        }

        /* Animation Classes */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <!-- Floating Flowers -->
    <div class="floating-flowers">
        <div class="flower-particle">🌸</div>
        <div class="flower-particle">🌺</div>
        <div class="flower-particle">🌼</div>
        <div class="flower-particle">🌻</div>
        <div class="flower-particle">🌷</div>
        <div class="flower-particle">🌸</div>
        <div class="flower-particle">🌺</div>
        <div class="flower-particle">🌼</div>
        <div class="flower-particle">🌻</div>
        <div class="flower-particle">🌷</div>
    </div>

    <!-- Audio Player -->
    <div class="audio-player">
        <audio controls autoplay loop>
            <source src="/audio/naff-akhirnyaku-menemukanmu.mp3" type="audio/mpeg">
            Your browser does not support the audio element.
        </audio>
    </div>

    <div class="main-container">
        <!-- Hero Section -->
        <section class="hero-section" id="hero">
            <div class="hero-content">
                <div class="invitee-name">Kepada Yth. Zahra</div>
                <div class="quran-verse">
                    "وَمِنْ آيَاتِهِ أَنْ خَلَقَ لَكُم مِّنْ أَنفُسِكُمْ أَزْوَاجًا لِّتَسْكُنُوا إِلَيْهَا وَجَعَلَ بَيْنَكُم مَّوَدَّةً وَرَحْمَةً"
                </div>
                <div class="wedding-title">THE WEDDING OF</div>
                <div class="couple-names"><%= bride %> & <%= groom %></div>
                <div class="wedding-date"><%= date %></div>
                
                <div class="countdown" id="countdown">
                    <div class="countdown-item">
                        <span class="countdown-number" id="days">00</span>
                        <span class="countdown-label">Hari</span>
                    </div>
                    <div class="countdown-item">
                        <span class="countdown-number" id="hours">00</span>
                        <span class="countdown-label">Jam</span>
                    </div>
                    <div class="countdown-item">
                        <span class="countdown-number" id="minutes">00</span>
                        <span class="countdown-label">Menit</span>
                    </div>
                    <div class="countdown-item">
                        <span class="countdown-number" id="seconds">00</span>
                        <span class="countdown-label">Detik</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- Section Navigation -->
        <div class="section-nav">
            <button class="nav-btn active" onclick="scrollToSection('couple')">Couple</button>
            <button class="nav-btn" onclick="scrollToSection('event')">Event</button>
            <button class="nav-btn" onclick="scrollToSection('gallery')">Gallery</button>
            <button class="nav-btn" onclick="scrollToSection('rsvp')">RSVP</button>
            <button class="nav-btn" onclick="scrollToSection('wish')">Wish</button>
            <button class="nav-btn" onclick="scrollToSection('gift')">Gift</button>
        </div>

        <!-- Couple Section -->
        <section class="section fade-in" id="couple">
            <h2 class="section-title">The Happy Couple</h2>
            <div class="couple-grid">
                <div class="couple-card">
                    <img src="/images/bride_aqeela.jpeg" alt="<%= bride %>" class="couple-photo">
                    <h3 class="couple-name"><%= bride %></h3>
                    <p class="couple-title">The Bride</p>
                    <p class="couple-parents">Putri dari<br>Bapak & Ibu</p>
                </div>
                <div class="couple-card">
                    <img src="/images/bride_mohan.jpeg" alt="<%= groom %>" class="couple-photo">
                    <h3 class="couple-name"><%= groom %></h3>
                    <p class="couple-title">The Groom</p>
                    <p class="couple-parents">Putra dari<br>Bapak & Ibu</p>
                </div>
            </div>
        </section>

        <!-- Event Section -->
        <section class="section fade-in" id="event">
            <h2 class="section-title">Event Details</h2>
            <div class="event-card">
                <div class="event-date"><%= date %></div>
                <div class="event-time">Pukul 11.00 WIB - Selesai</div>
                <div class="event-location">
                    Bertempat di<br>
                    <a href="<%= typeof locationUrl !== 'undefined' ? locationUrl : '#' %>" target="_blank" class="location-link">
                        <%= location %>
                    </a>
                </div>
                <div class="event-location">
                    <strong>Dress Code:</strong><br>
                    Pastel Colors
                </div>
            </div>
        </section>

        <!-- Gallery Section -->
        <section class="section fade-in" id="gallery">
            <h2 class="section-title">Galeri Foto</h2>
            <div class="gallery-grid">
                <div class="gallery-item">
                    <img src="/images/bride_aqeela.jpeg" alt="Foto Pengantin">
                </div>
                <div class="gallery-item">
                    <img src="/images/bride_mohan.jpeg" alt="Foto Pengantin">
                </div>
                <div class="gallery-item">
                    <img src="/images/aqeela.jpeg" alt="Foto Pengantin">
                </div>
                <div class="gallery-item">
                    <img src="/images/Mohan.jpeg" alt="Foto Pengantin">
                </div>
                <% if (images.length > 0) { %>
                    <% images.forEach(function(img) { %>
                        <div class="gallery-item">
                            <img src="/images/<%= img %>" alt="Foto Pengantin">
                        </div>
                    <% }); %>
                <% } %>
            </div>
        </section>

        <!-- RSVP Section -->
        <section class="section fade-in" id="rsvp">
            <h2 class="section-title">RSVP</h2>
            <form action="/rsvp" method="POST" class="rsvp-form">
                <div class="mb-3">
                    <input type="text" class="form-control" name="nama" placeholder="Nama Anda" required>
                </div>
                <div class="mb-3">
                    <input type="number" class="form-control" name="jumlah" placeholder="Jumlah Tamu" min="1" value="1" required>
                </div>
                <div class="mb-3">
                    <select class="form-control" name="kehadiran" required>
                        <option value="">Pilih Konfirmasi Kehadiran</option>
                        <option value="Hadir">Hadir</option>
                        <option value="Tidak Hadir">Tidak Hadir</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary w-100">
                    <i class="fas fa-paper-plane me-2"></i>Kirim RSVP
                </button>
            </form>
        </section>

        <!-- Wish Box Section -->
        <section class="section fade-in" id="wish">
            <h2 class="section-title">Wish Box</h2>
            <div class="wish-box">
                <form action="/wish" method="POST">
                    <div class="mb-3">
                        <input type="text" class="form-control" name="nama" placeholder="Nama Anda" required>
                    </div>
                    <div class="mb-3">
                        <textarea class="form-control wish-input" name="pesan" rows="4" placeholder="Tulis ucapan dan doa untuk kami..." required></textarea>
                    </div>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-heart me-2"></i>Kirim Ucapan
                    </button>
                </form>
            </div>
        </section>

        <!-- Gift Section -->
        <section class="section fade-in" id="gift">
            <h2 class="section-title">Wedding Gift</h2>
            <div class="gift-info">
                <h3 class="gift-title">Doa Restu Anda</h3>
                <p class="gift-text">
                    Doa dan restu dari Anda adalah hadiah terindah untuk kami.<br>
                    Namun jika ingin memberikan hadiah, dapat melalui rekening berikut:
                </p>
                <div class="bank-info">
                    <div class="bank-name">Bank Central Asia (BCA)</div>
                    <div class="bank-number">123-456-7890</div>
                    <div class="bank-name">a.n. Aqeele</div>
                </div>
                <div class="bank-info">
                    <div class="bank-name">Bank Mandiri</div>
                    <div class="bank-number">098-765-4321</div>
                    <div class="bank-name">a.n. Mohan</div>
                </div>
            </div>
        </section>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.js"></script>
    <script>
        // Countdown Timer
        function updateCountdown() {
            const weddingDate = new Date('August 8, 2025 11:00:00').getTime();
            const now = new Date().getTime();
            const distance = weddingDate - now;
            
            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);
            
            document.getElementById('days').innerHTML = days.toString().padStart(2, '0');
            document.getElementById('hours').innerHTML = hours.toString().padStart(2, '0');
            document.getElementById('minutes').innerHTML = minutes.toString().padStart(2, '0');
            document.getElementById('seconds').innerHTML = seconds.toString().padStart(2, '0');
        }
        
        setInterval(updateCountdown, 1000);
        updateCountdown();

        // Smooth Scroll to Section
        function scrollToSection(sectionId) {
            const section = document.getElementById(sectionId);
            if (section) {
                section.scrollIntoView({ behavior: 'smooth' });
                
                // Update active nav button
                document.querySelectorAll('.nav-btn').forEach(btn => btn.classList.remove('active'));
                event.target.classList.add('active');
            }
        }

        // Scroll Animation
        function checkScroll() {
            const elements = document.querySelectorAll('.fade-in');
            elements.forEach(element => {
                const elementTop = element.getBoundingClientRect().top;
                const elementVisible = 150;
                if (elementTop < window.innerHeight - elementVisible) {
                    element.classList.add('visible');
                }
            });
        }

        // Update active nav based on scroll position
        function updateActiveNav() {
            const sections = ['hero', 'couple', 'event', 'gallery', 'rsvp', 'wish', 'gift'];
            const navButtons = document.querySelectorAll('.nav-btn');
            
            sections.forEach((sectionId, index) => {
                const section = document.getElementById(sectionId);
                if (section) {
                    const rect = section.getBoundingClientRect();
                    if (rect.top <= 100 && rect.bottom >= 100) {
                        navButtons.forEach(btn => btn.classList.remove('active'));
                        navButtons[index]?.classList.add('active');
                    }
                }
            });
        }

        window.addEventListener('scroll', () => {
            checkScroll();
            updateActiveNav();
        });
        
        checkScroll();
    </script>
</body>
</html> 
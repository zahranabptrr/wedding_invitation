<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Undangan Pernikahan Aqeele & Mohan</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Montserrat:wght@300;400;600&display=swap');
        body {
            font-family: 'Montserrat', 'Poppins', sans-serif;
            background: url('/images/background.jpg') no-repeat center center fixed;
            background-size: cover;
            min-height: 100vh;
            position: relative;
            overflow-x: hidden;
        }
        body::before {
            content: '';
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            background: linear-gradient(135deg, #f5e9d7cc 0%, #e7d3c0cc 100%);
            z-index: 1;
            pointer-events: none;
        }
        .floral-corner {
            position: fixed;
            z-index: 2;
            pointer-events: none;
        }
        .floral-corner.top-right {
            top: 0; right: 0;
            width: 300px;
            height: 300px;
            background: url('/images/background.jpg') no-repeat right top;
            background-size: 300px 300px;
            opacity: 0.7;
        }
        .floral-corner.bottom-left {
            bottom: 0; left: 0;
            width: 300px;
            height: 300px;
            background: url('/images/background.jpg') no-repeat left bottom;
            background-size: 300px 300px;
            opacity: 0.7;
        }
        .welcome-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            padding: 20px;
            z-index: 3;
        }
        .welcome-card {
            background: rgba(255, 255, 255, 0.92);
            border-radius: 32px;
            padding: 4rem 3rem;
            text-align: center;
            box-shadow: 0 25px 50px rgba(139, 69, 19, 0.13);
            backdrop-filter: blur(16px);
            max-width: 500px;
            width: 100%;
            position: relative;
            overflow: hidden;
            border: 2px solid #e2c9b0;
        }
        .welcome-card::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 5px;
            background: linear-gradient(90deg, #bfa181, #8B4513, #e7d3c0, #bfa181);
            background-size: 200% 100%;
            animation: gradientMove 3s ease-in-out infinite;
        }
        .welcome-card::after {
            content: '🌸 🌺 🌸';
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 1.5rem;
            opacity: 0.5;
            animation: flowerFloat 4s ease-in-out infinite;
        }
        @keyframes gradientMove {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }
        @keyframes flowerFloat {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-10px) rotate(5deg); }
        }
        .recipient-name {
            font-family: 'Playfair Display', serif;
            font-size: 3rem;
            font-weight: 700;
            color: #8B4513;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, #8B4513, #bfa181);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 2px 2px 4px rgba(139, 69, 19, 0.08);
        }
        .welcome-text {
            font-size: 1.3rem;
            color: #8B4513;
            margin-bottom: 2.5rem;
            line-height: 1.6;
            font-weight: 400;
        }
        .couple-names {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            color: #bfa181;
            margin-bottom: 2rem;
            font-weight: 600;
        }
        .open-btn {
            background: linear-gradient(45deg, #bfa181, #8B4513);
            border: none;
            padding: 18px 50px;
            font-size: 1.2rem;
            border-radius: 50px;
            color: white;
            text-decoration: none;
            transition: all 0.4s ease;
            display: inline-block;
            font-weight: 500;
            position: relative;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(139, 69, 19, 0.18);
        }
        .open-btn::before {
            content: '';
            position: absolute;
            top: 0; left: -100%; width: 100%; height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }
        .open-btn:hover::before { left: 100%; }
        .open-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(139, 69, 19, 0.25);
            color: white;
        }
        @media (max-width: 600px) {
            .welcome-card { padding: 2rem 0.7rem; }
            .recipient-name { font-size: 2rem; }
            .couple-names { font-size: 1.2rem; }
        }
    </style>
</head>
<body>
    <div class="floral-corner top-right"></div>
    <div class="floral-corner bottom-left"></div>
    <div class="welcome-container">
        <div class="welcome-card">
            <div class="recipient-name">Zahra</div>
            <div class="welcome-text">Kepada Yth.<br> Bapak/Ibu/Saudara/i</div>
            <div class="couple-names">Aqeele &amp; Mohan</div>
            <a href="/invitation" class="open-btn">Buka Undangan</a>
        </div>
    </div>
</body>
</html> 